[package]
name = "bootloader"
version = "0.1.0"
edition = "2024"

[dependencies]
uefi = { version = "0.36.1", default-features = false }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[build]
target = "x86_64-unknown-uefi"
